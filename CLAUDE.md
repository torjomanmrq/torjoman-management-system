# ترجمان القرآني — Project Rules for Claude Code

> منصّة قرآنية لإدارة حفظ القرآن وفهمه وتدبّره ضمن منظومة تربوية متكاملة.
> أربعة أدوار عاملة + الطالب سجلّ بيانات · ~24 شاشة · نموذج صلاحيات هرمي.

---

## المراجع الأساسية

اقرأ هذه الملفات **في بداية كل جلسة قبل كتابة أي كود**:

- خريطة الطريق والأولويات: `./docs/ROADMAP.md`
- الوظائف وسجل التنفيذ: `./docs/FEATURES.md`
- قاعدة البيانات: `./docs/DATABASE.md`
- حالة الصفحات: `./docs/PAGES.md`
- البريف التصميمي الكامل (مرجع بصري): `./docs/DESIGN-BRIEF.md`

> **مبدأ حاكم:** البريف التصميمي مرجع بصري فقط. لا تنسخ منه كوداً جاهزاً — أعد بناء كل شاشة كـ Nuxt حقيقي نظيف وفق المعايير أدناه.

---

## الهوية البصرية

- **اللون الأساسي (Navy):** `#094064` — الهيدر، الـ Sidebar، الأزرار الرئيسية، العناوين
- **اللون الثانوي (Blue):** `#4C8BC9` — الروابط، العناصر التفاعلية الثانوية، التمييز
- **اللون الثالث (Green):** `#54BF99` — النجاح، التقدّم الإيجابي، شارات الحالة
- **الخط:** Lama Sans (المعتمد) مع IBM Plex Sans Arabic احتياطياً
- **الأوزان:** Light (300) / Medium (500) / SemiBold (600) — يُربط 400 بـ Medium و 700 بـ SemiBold
- **الاتجاه:** RTL بالكامل — لا استثناء
- **الأوضاع:** فاتح + ليلي (Light + Dark). في الليلي: نسخة اللوغو البيضاء على خلفية Navy
- **الأسلوب:** هادئ رصين يليق بسياق قرآني — مساحات بيضاء مريحة، حواف ناعمة، تباين واضح، تجنّب الزخرفة الزائدة

---

## التقنية

- **Framework:** Nuxt 4
- **Language:** TypeScript (strict)
- **UI Library:** @nuxt/ui v4
- **State:** Pinia
- **Database / Auth:** Supabase
- **Hosting:** Vercel
- **Pattern:** Vue 3 Composition API **فقط** — لا Options API إطلاقاً

---

## نظام الأدوار

المنصّة **مغلقة** — لا تسجيل ذاتي، و**الطالب ليس مستخدماً** (سجلّ بيانات بلا حساب ولا دخول، يُدخله المعلم). الحسابات للأدوار الأربعة العاملة فقط، وتُنشأ كالتالي:

| الدور                     | المفتاح         | معرّف الدخول      | كيف يُنشأ                    |
| ------------------------- | --------------- | ----------------- | ---------------------------- |
| **manager** (المدير)      | صلاحية كاملة    | البريد الإلكتروني | دعوة بريدية                  |
| **quality** (مشرف الجودة) | يدير مشرفيه     | البريد الإلكتروني | المدير فقط (دعوة)            |
| **supervisor** (المشرف)   | الطاقم الميداني | البريد الإلكتروني | المدير فقط (دعوة)            |
| **teacher** (المعلم)      | حلقة واحدة      | البريد الإلكتروني | المدير فقط (دعوة)            |
| الطالب                    | سجلّ بيانات     | — (لا دخول)       | يُدخله المعلم (بلا حساب)     |

**التسلسل الهرمي:** المدير ← مشرف الجودة ← المشرف ← المعلم ← الطالب (سجلّ بيانات).

> **ملاحظة على الطلاب:** الطالب **بيانات مجردة لا حساب له في Supabase Auth ولا يدخل النظام**. رقم الهوية حقل تعريفي فقط (لا للدخول)، والبريد اختياري لإشعار ولي الأمر لا للاستعادة. كل ما يخصّه (حفظ، اختبارات، رحلة) يُدار ويُعرض من العاملين داخل ملف الطالب.

### مصفوفة الصلاحيات (مرجع إلزامي لسياسات RLS)

| الإجراء                              | المدير | مشرف الجودة | المشرف      | المعلم     |
| ------------------------------------ | ------ | ----------- | ----------- | ---------- |
| تعيين/عزل مشرف جودة + ربطه بنطاقه    | ✅ فقط | ❌          | ❌          | ❌         |
| إنشاء/تعديل حساب معلم أو مشرف ميداني | ✅     | ❌          | ❌          | ❌         |
| إسناد حلقة لمشرف ميداني              | ✅     | ✅ (نطاقه)  | ❌          | ❌         |
| إدخال بيانات الطلاب (سجلّ بلا حساب)  | ✅     | ❌          | ❌          | ✅ (حلقته) |
| تعديل بيانات الطلاب                  | ✅     | ❌          | ✅ (حلقاته) | ✅ (حلقته) |
| متابعة المشرفين وزياراتهم            | ✅     | ✅ (مشرفوه) | ❌          | ❌         |
| إعادة تعيين كلمات المرور (طوارئ)     | ✅ فقط | ❌          | ❌          | ❌         |
| البيانات المالية                     | ✅ فقط | ❌          | ❌          | ❌         |

> **مبدأ التعيين:** لا يُعيّن أحدٌ من يراقبه. كل تعيين يُسجَّل (مَن عيّن مَن ومتى) — عمود «عيّنه».

---

## قواعد كتابة الكود

### الجودة العامة

- كل component موثّق بـ JSDoc في أعلى الملف يشرح غرضه
- كل function لها اسم واضح يصف ما تفعله بالضبط
- لا magic numbers — استخدم constants مسماة في ملف منفصل (`app/constants/`)
- لا كود مكرر — استخرج أي منطق مشترك إلى composable
- الملفات لا تتجاوز **300 سطر** — إذا تجاوزت، قسّمها
- **Separation of concerns:** لا منطق أعمال داخل المكوّنات — يُنقل إلى composables أو stores

### TypeScript

- كل props موثّقة بـ TypeScript interfaces
- استخدم `defineProps` و `defineEmits` بشكل صريح ومُنمّط
- لا `any` إلا في حالات استثنائية موثّقة بتعليق يبرّرها
- **أنواع قاعدة البيانات تُولَّد فقط** عبر `supabase gen types typescript` إلى `app/types/database.types.ts` — **يُمنع كتابتها يدوياً**
- كل وصول لـ Supabase مُنمّط عبر client يحمل نوع `Database`

### Composables

- كل منطق متكرر يُستخرج إلى composable في `app/composables/`
- اسم الـ composable يبدأ بـ `use` (مثال: `useAuth`, `useHalaqa`, `useScope`)
- كل composable له ملف اختبار مقابل (`*.test.ts`)

### CSS والتصميم

- استخدم CSS variables، لا قيم لونية مباشرة
- لا hardcoded colors خارج `app/assets/css/main.css`
- استخدم Tailwind classes المتوافقة مع @nuxt/ui v4
- كل المكوّنات **RTL-aware** — انتبه للأيقونات الاتجاهية (لا تنعكس خطأً)
- أحجام اللمس لا تقلّ عن 44×44px على الجوال

### الأداء

- Lazy-load للمكوّنات الثقيلة وقوائم التفاصيل (انظر أنماط القوائم في البريف §4.16)
- استخدم `computed` للحالة المشتقّة بدل دوال تُستدعى في القالب
- تجنّب watchers غير الضرورية
- الجداول الكبيرة: تحميل عند الطلب (Deferred filtering) — طلب واحد عند ضغط «عرض النتائج»، لا طلب لكل تغيير فلتر
- استعلامات التجميع محورها الكيان الأخفّ (مثال: زيارات محورها الحلقة عبر `GROUP BY`)

---

## قواعد الأمن

- **لا بيانات حساسة في الكود** — استخدم `.env` دائماً
- متغيّرات الخادم لا تُسبق بـ `PUBLIC` ولا تُعرض للعميل (خاصة `service_role`)
- كل route محمي بـ middleware يتحقّق من الدور
- التحقّق من الصلاحيات في **الخادم** (server side / RLS) وليس فقط العميل
- **RLS إلزامي:** كل جدول جديد يُفعّل عليه Row Level Security **فور إنشائه** — لا جدول بدون سياسات
- العمليات التي تتطلّب `service_role` (إعادة تعيين كلمة مرور، إنشاء حساب بدعوة) تُنفّذ في **Edge Function** تتحقّق من هوية المستدعي أولاً
- تحقّق من صحة كل input قبل إرساله لقاعدة البيانات
- **رقم الهوية** يُعرض مقنّعاً (آخر 4 أرقام) لغير أصحاب الصلاحية

---

## معالجة الأخطاء

- معالجة موحّدة عبر composable واحد (`useErrorHandler`) يعرض رسالة عربية واضحة عبر `useToast`
- لا معالجة عشوائية متفرّقة (`try/catch` فارغة أو `console.log` فقط)
- رسائل الخطأ للمستخدم بالعربية دائماً

---

## قواعد SEO

- كل صفحة عامة لها `useHead()` مع title و description بالعربية
- الصور لها `alt` text دائماً
- استخدم semantic HTML (`<main>`, `<nav>`, `<article>`, `<section>`)
- العناوين متسلسلة (h1 → h2 → h3) بلا قفز

---

## قواعد التعديل والحذف

- **قبل أي تعديل:** ابحث عن كل مكان يستخدم هذا الـ component أو الـ function (global search)
- **قبل أي حذف:** تأكّد بـ global search أن الكود غير مستخدم في أي مكان آخر
- **بعد كل تعديل:** تحقّق أن TypeScript لا يُظهر أخطاء
- **لا تكسر وظيفة قائمة** لإضافة وظيفة جديدة
- **لا تعدّل ملفات غير مذكورة في الطلب**
- **فضّل تعديل الموجود على إعادة الكتابة من الصفر**

---

## سير العمل (Workflow)

- **عند أي غموض في الطلب: اسأل قبل التنفيذ — لا تفترض**
- إذا كانت المهمة كبيرة: قسّمها إلى خطوات وأكّدها قبل المتابعة
- تحقّق ممّا هو موجود قبل إنشاء شيء جديد
- لا تكتب كوداً أكثر ممّا طُلب

---

## الاختبار (Testing)

- **Vitest** لاختبار الـ composables والمنطق (وحدات)
- **Playwright** للمسارات الحرجة (E2E): تسجيل الدخول، إنشاء مستخدم، إدخال نتيجة اختبار، جدولة زيارة
- كل composable جديد يصاحبه ملف اختبار

---

## قواعد Git

- **الفروع:**
  - `main` — إنتاج (مستقر فقط)
  - `dev` — تكامل
  - `feature/*` — ميزة واحدة لكل فرع (مثال: `feature/user-management`)
- **ممنوع الـ commit مباشرة على `main`** — كل دمج عبر Pull Request
- **Conventional Commits:** `feat:` / `fix:` / `refactor:` / `chore:` / `docs:` / `test:`
- **commit صغير ومركّز** — تغيير منطقي واحد لكل commit
- **لا يُرفع `.env` أبداً** — تأكّد من `.gitignore`
- دورة الميزة: `dev` → `feature/x` → (عمل + commits) → PR → `dev` → (عند الاستقرار) → `main`

---

## ⚠️ إلزامي بعد كل تعديل في الكود

1. **حدّث `./docs/FEATURES.md`** — ما الذي نُفّذ + ملخّص التغيير في الـ Changelog
2. **حدّث `./docs/PAGES.md`** — حالة الشاشة المعنيّة (لم تبدأ / قيد العمل / مكتملة)
3. **إذا تغيّر مخطط قاعدة البيانات:**
   - أنشئ migration جديداً (لا تعديل يدوي مباشر على القاعدة)
   - أعد توليد الأنواع (`npm run db:types`)
   - حدّث `./docs/DATABASE.md` ليطابق آخر migration
4. اكتب رسالة commit وفق Conventional Commits

---

## هيكل المشروع (Nuxt 4)

```
turjuman/
├── app/
│   ├── assets/css/main.css      ← CSS variables والهوية البصرية
│   ├── components/              ← مكوّنات قابلة لإعادة الاستخدام
│   │   ├── ui/                  ← الأساسيات (أزرار، حقول، بطاقات…)
│   │   └── [feature]/           ← مكوّنات خاصة بكل ميزة
│   ├── composables/             ← منطق مشترك (useAuth, useScope…)
│   ├── constants/               ← القيم الثابتة المسماة
│   ├── layouts/                 ← تخطيطات (RTL Shell)
│   ├── middleware/              ← حماية الـ routes حسب الدور
│   ├── pages/                   ← صفحات المنصّة (routing تلقائي)
│   ├── stores/                  ← Pinia
│   └── types/                   ← TypeScript + database.types.ts (مولّد)
├── server/                      ← API + Edge Functions (service_role)
├── supabase/migrations/         ← migrations مرقّمة (مصدر تغيير القاعدة)
├── docs/                        ← توثيق المشروع
│   ├── ROADMAP.md
│   ├── FEATURES.md
│   ├── DATABASE.md
│   ├── PAGES.md
│   └── DESIGN-BRIEF.md
└── CLAUDE.md                    ← هذا الملف
```
