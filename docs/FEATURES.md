# سجل الوظائف — ترجمان القرآني

> يحدّثه Claude Code **بعد كل تعديل في الكود**: ما الذي نُفّذ + ما التغييرات.
> الترتيب في الـ Changelog: الأحدث أولاً.

---

## مكتمل ✅

- **[الإعداد]** سَقالة Nuxt 4 + Nuxt UI v4 + Tailwind 4 + Pinia + Supabase، بنية مجلدات CLAUDE.md، ضبط RTL/عربي، `.env` آمن — 2026-06-22
- **[نظام التصميم]** توكنز البريف §5 (فاتح/ليلي)، خط Lama Sans، تدرّجات الألوان مرتبطة بـ Nuxt UI — 2026-06-22

---

## قيد العمل 🟡

_(لا شيء بعد — التالي: المكوّنات الأساسية §5، ثم أول migration.)_

---

## مخطّط ⬜

الميزات الكاملة موثّقة في `ROADMAP.md` مقسّمةً على ثلاث مراحل. ملخّص النطاق الحالي (المرحلتان 1 و2):

**المرحلة 1 — الأساس:**
- نظام التصميم (Tokens، RTL، الوضعان، المكوّنات الأساسية)
- المصادقة (دخول بالبريد للعاملين فقط، دعوة بريدية، إعادة تعيين) — الطالب لا يدخل النظام
- الهيكل العام (Shell + Sidebar + Drawer)
- حماية المسارات حسب الدور
- إدارة المستخدمين والحلقات
- الملفات الشخصية
- لوحات الأدوار الأربع العاملة
- محاضر الاجتماعات + الملف المالي + الأخبار

**المرحلة 2 — العمليات الميدانية:**
- الزيارات الإشرافية (جدولة/نتائج/متابعة)
- الاختبارات (ترشيح/تقييم/خطة يعدّلها المدير)
- التقارير الشهرية + كشف حوافز الحلقة
- مؤشرات التقدّم
- أدلّة الطلاب والمشرفين
- التنبيهات الإدارية
- دليل المعادلات

---

## Changelog

> سجلّ زمني لكل تغيير: ماذا تغيّر ولماذا. الأحدث أولاً.

<!-- مثال:
### 2026-01-15
- **feat:** إضافة composable `useAuth` لدخول العاملين بالبريد
- **chore:** إعداد Supabase client مُنمّط بنوع Database
-->

### 2026-06-25
- **fix(exams):** إعادة تصميم نموذج رصد النتيجة — كل سؤال بتفريعاته (حفظ/فهم/تدبّر) في مجموعة منفصلة + التجويد، حقول أكبر (٣/صفّ، أطول وأوضح) مع حدّ صارم ١٠ لكل فرع (لا يقبل أكثر)
- **fix(ui):** شارة عدّاد جرس الإشعارات أكبر وأوضح (22px، خط عريض، إطار أبيض) بدل UChip الصغير
- **fix(nav):** إزالة تكرار تبويب المعلّم — كان «حلقتي» و«لوحة التحكم» يشيران لـ/dashboard فيُضاءان معاً؛ حُذف «حلقتي» (لوحة المعلّم نفسها هي حلقته)
- **feat(export):** ترقية التصدير إلى ملفّ .xlsx حقيقي (exceljs محمّل ديناميكيّاً) — ورقة RTL + رأس ملوّن عريض + عرض أعمدة تلقائي، في الحركات المالية والتقرير الشهري
- **feat(reports):** بطاقات مراجعة التقارير (§4.17د) — قائمة الحلقات صارت بطاقات تُبرز إنجاز الحلقة (رقم/تقدير/شريط ملوّن) + الانتظام والأنشطة، والنقر يفتح التقرير للاعتماد
- **feat(ui):** ترقيم صفحات مشترك (§4.16ج) — مكوّن `UiPaginator` + composable `usePagination`، مُطبّق على إدارة المستخدمين والطلاب (12/صفحة) والحلقات (9/صفحة) مع إعادة للصفحة الأولى عند تغيير الفلتر
- **feat(finance):** نظرة عامة محسّنة (§4.20أ) — أسهم اتجاه على بطاقات الرصيد/الوارد/الصادر (مقارنةً بالشهر السابق) + منحنى رصيد لآخر 6 أشهر (SVG نقيّ بلا مكتبات)
- **feat(storage):** رفع الصور إلى Supabase Storage (باكت media عامّ) عبر composable `useUpload` — صور الأخبار (§4.26) والصورة الشخصية في الملف (§4.5) بدل الروابط، مع معاينة وحدّ 5MB
- **feat(export):** تصدير Excel (CSV بترميز UTF-8 يفتح في Excel) عبر composable `useExport` — في سجلّ الحركات المالية (§4.20ب) والتقرير الشهري (§4.17)
- **feat(users):** إعادة تعيين كلمة المرور الطارئة من المدير (§4.12) — مسار خادمي بصلاحية الخدمة (updateUserById) يضبطها على الافتراضية ويرفع علَم التغيير + زرّ وتأكيد في إدارة المستخدمين
- **chore(security):** ملف هجرة لتثبيت search_path لدوال التريجر (إصلاح تحذير المستشار؛ يُطبَّق بإذن)

### 2026-06-24
- **feat(students):** مؤشّر التقدّم الزمني (§4.18) — مكوّن `TimeProgress` يقارن حفظ الطالب الفعلي بالمتوقّع زمنيّاً منذ الالتحاق (الأشهر × الهدف الشهري) ويعرض متقدّم/في المسار/متأخّر، ضمن رحلة الطالب (الملف الداخلي + الرابط العام)
- **feat(supervisors):** دليل المشرفين وتفاصيله (§4.14) — للمدير/الجودة (ضمن النطاق): بطاقات المشرفين (عدد الحلقات + نسبة الالتزام بالزيارات)، وصفحة تفاصيل (حلقاته ومعلّموها + الالتزام + آخر الزيارات). وربط تبويبَي «المشرفون» -> /supervisors و«حلقتي» -> /dashboard
- **feat(grades):** حساب المؤشرات (§4.24) — composable `useGrades` نقيّ (درجة الطالب 60/20/20 + إنجاز الحلقة 80/20 + التقدير + ألوان الشارة)؛ في التقارير: درجة كل طالب محسوبة تلقائيّاً (بدل إدخال يدوي) + بطاقة «إنجاز الحلقة» (رقم/تقدير/شريط ملوّن) + **لوحة التكريم** (أعلى الطلاب من التقارير المعتمدة) للمدير/الجودة
- **feat(auth):** صفحة `auth/confirm` لضبط كلمة المرور بعد رابط الاستعادة (§4.1) — تُكمل دورة «نسيت كلمة المرور» المعطّلة سابقاً (الرابط كان يحوّل لوجهة غير موجودة)؛ تعرض النموذج عند وجود جلسة استعادة وإلا «رابط غير صالح»
- **feat(auth):** كلمة سرّ افتراضية موحّدة لحسابات العاملين (DEFAULT_PASSWORD، تُملأ تلقائيّاً في نموذج الإنشاء + علَم must_change_password) + زرّ «تغيير كلمة المرور» في الملف الشخصي (للعامل) + بانر تنبيه ببقاء الكلمة الافتراضية
- **feat(finance):** الملف المالي (§4.20) — للمدير، 4 تبويبات: نظرة عامة (رصيد/وارد/صادر + توزّع المصروفات) · سجلّ الحركات (رصيد محسوب + إضافة/تعديل/حذف + فلاتر) · الرواتب الشهرية (إدخال/اعتماد بقفل/ترحيل كحركة) · حوافز الحلقات (تخصيص بقفل يصل لكشف المعلم §4.25 + جدول الكشوف الواردة مع «تمّ الاطّلاع»)
- **feat(incentives):** كشف حوافز الحلقة الشهري (§4.25) — للمعلّم: حلقته تلقائيّاً + اختيار شهر/سنة، المبلغ المخصّص (للقراءة من تخصيص المدير) + بنود مصروفات (بيان/تصنيف/مبلغ/تاريخ) مع إجمالي ومتبقّي وتحذير التجاوز، وحالة مسودّة→مُرسل (RLS لحلقة المعلّم)
- **feat(profile):** الملف الشخصي للعامل (§4.4/4.5) — `/profile/[id]` (و«me» للمستخدم الحالي): ترويسة + نطاق حسب الدور + أقسام (أساسية/وظيفية/سكن/مؤهلات) + تعديل (للمدير أو صاحب الملف عبر RLS)؛ رقم الهوية مقنّع لغير المخوّل؛ وزرّ «عرض البيانات» في إدارة المستخدمين
- **feat(minutes):** محاضر الاجتماعات (§4.19) — للمدير: إنشاء/تعديل/حذف + قراءة، الحاضرون يُختارون من العاملين المؤهّلين (مدير/جودة/مشرف) كشرائح، جدول أعمال/قرارات/مهام، وحالة مسودّة/نهائي
- **feat(news):** وحدة الأخبار (§4.26) — إدارة للمدير (إنشاء/تعديل/حذف + نشر/إخفاء + قراءة + رابط صورة)، وربط صفحة الهبوط بأحدث ٣ أخبار منشورة حقيقية بدل البيانات الوهمية (قراءة عامّة للزوّار عبر RLS)
- **feat(metrics):** دليل المؤشرات والمعادلات (§4.24) — مرجع للمدير/الجودة بأربعة أقسام (الأداء/الزمن/المالية/الثوابت)؛ كل بطاقة: تعريف + صيغة + كيف تُقرأ + مثال محسوب + المصدر، والثوابت بقيمها الحالية من app_settings
- **feat(notifications):** جرس إشعارات محسوب في الشريط العلوي (بلا جدول) — حسب الدور: تنبيهات جديدة (معلّم) / بنود رصد + زيارات اليوم (مشرف) / تقارير بانتظار الاعتماد (مدير/جودة)؛ شارة عدد + قائمة منسدلة بروابط، تحديث عند التنقّل + استطلاع كل 60ث
- **feat(dashboard):** لوحة الجودة — مشرفوها (عدد حلقات كل مشرف) + الحلقات في نطاقها + زيارات قادمة + تقارير بانتظار الاعتماد (RLS عبر quality_supervisor_id)
- **feat(alerts):** التنبيهات الإدارية (§4.21) — المدير/الجودة/المشرف يُصدرون تنبيهاً لمعلّم (نوع المخالفة + درجة note/إنذار أول/نهائي + وصف)، والمعلّم يستقبله ويؤكّد الاطّلاع؛ النطاق محكوم بـ RLS
- **feat(halqat):** صفحة تفاصيل الحلقة (§4.14) — ترويسة (معلّم/مشرف/وقت/تصنيف) + طلابها بتقدّمهم (الأجزاء + المحطة القادمة) + آخر تقرير شهري + آخر الزيارات؛ تُفتح بزرّ «تفاصيل» من بطاقة الحلقة (لكل الأدوار ضمن نطاقهم)
- **fix(halqat):** صفحة الحلقات صارت عرضاً للاطّلاع للمشرف/الجودة ضمن نطاقهم (RLS) بدل قفل «للمدير فقط» — الإنشاء/التعديل/الحذف تبقى للمدير
- **feat(dashboard):** لوحة المشرف — حلقاته (المعلّم + عدد الطلاب) + زياراته المجدولة القادمة + عدّاد الاختبارات بانتظار الرصد مع روابط سريعة (/visits و/exams)
- **feat(dashboard):** لوحة المعلّم «حلقتي» — حلقته تلقائيّاً + طلابها (الأجزاء المثبتة + المحطة المستحقّة القادمة) + إجراءات سريعة (ترشيح للاختبار / تقرير الشهر) ورابط ملف كل طالب
- **feat(students):** إجمالي صفحات الحفظ في رحلة الطالب — يُجمَع من التقارير الشهرية المعتمدة ويظهر كمؤشّر (الملف الداخلي + الرابط العام)
- **fix(reports):** أدوار التقارير — المعلّم تأتي حلقته تلقائيّاً (لا يختارها)، والمدير لا يُنشئ/يحرّر بل يستقبل من القائمة ويعتمد فقط (عرض للقراءة)
- **feat(reports):** قائمة استعراض التقارير — المدير يرى كل تقارير الحلقات (تصفية بالحالة، إبراز «بانتظار الاعتماد») وزرّ «فتح» يحمّل التقرير للمراجعة والاعتماد
- **feat(reports):** التقرير الشهري للحلقة (§4.17) — اختيار حلقة/شهر/سنة، جدول لكل طالب (حفظ من/إلى + مراجعة + غياب + نقاط + درجة، والصفحات محسوبة)، أنشطة مصاحبة (14)، ملاحظات، وحالة (مسودّة→مُرسل→معتمد). المعلّم يرفع والمدير يعتمد (RLS)
- **feat(students):** إظهار درجة الاختبار (مثل «نجح · 85/100») بجانب حالة المحطة في رحلة الطالب
- **feat(exams):** حرّاس الترشيح — منع ترشيح الطالب لاختبار اجتازه (يُستبعد من خياراته) ومنع التكرار قبل الرصد («بانتظار الرصد»)، مع تحقّق دفاعي عند الإرسال ورسائل واضحة
- **feat(students):** ربط نتائج الاختبارات برحلة الطالب — المحطات تُظهر «نجح/يعيد» بالعلامات (لا «بلغها» فقط)، وعدّاد «اجتازها»، في الملف الداخلي والرابط العام
- **feat(exams):** وحدة الاختبارات (§4.16) موجّهة بالدور — المعلّم يرشّح طلابه المستحقّين (حسب الحفظ من الخطة) ويرسل القائمة؛ المشرف/المدير يرصد النتيجة من 100 (10 خانات، المجموع والنجاح محسوبان في القاعدة)؛ قائمة النتائج للجميع (RLS)
- **feat(students):** رحلة الطالب على خطة الاختبارات (حسب الحفظ) — مكوّن مشترك `StudentJourney` (محطات الخطة + المُستحقّة القادمة + شريط تقدّم + «منذ الالتحاق») في الملف الداخلي والرابط العام. صفحات الحفظ والنتائج لاحقاً
- **feat(students):** رابط اطّلاع الطالب العام (§4.23) — عمود `view_token` + صفحة عامّة `/s/[token]` (للقراءة فقط بلا حساب) + مسار خادمي يُرجِع حقولاً آمنة فقط + زرّا «نسخ الرابط/إعادة توليد» في ملف الطالب

### 2026-06-23
- **feat(ui):** مكوّن `UiSelect` موحّد (يُضمّن توسيع القائمة لمنع قصّ النص + استدارة) واستُخدم في القوائم الطويلة (حلقة/معلّم/مشرف/زائر)
- **feat(students):** توسيع نموذج الطالب ليشمل كل الحقول (عدد الأفراد، الأحكام، السكن، أقرب مسجد، جوال الطالب، بريد وليّ الأمر، تاريخ الالتحاق)
- **feat(students):** ملف الطالب `students/[id].vue` (§4.23) — شريط علوي + بطاقات (أساسية/قرآنية/سكن/وليّ الأمر) + زرّ «عرض» في الدليل. قسم «رحلة الطالب» لاحقاً مع التقارير/الاختبارات
- **feat(visits):** إظهار «وقت الحلقة اليومي» كتلميح في نافذة جدولة الزيارة عند اختيار الحلقة
- **feat(students):** قائمة إسناد حلقة الطالب تعرض «الاسم — المعلّم — الوقت» (قائمة التصفية تبقى بالاسم فقط)
- **feat(visits):** خيارات قائمة الحلقة في نافذة الجدولة تعرض «الاسم — المعلّم — الوقت» بدل الاسم فقط
- **feat(visits):** إظهار دور الزائر (مدير/مشرف) كشارة في عمود «الزائر» بجدول الزيارات
- **fix(ui):** تجاوب الجداول على الجوال — `UiDataTable` يمرّر أفقيّاً (`overflow-x:auto` + حدّ أدنى للعرض ≤640px) بدل قصّ المحتوى. يفيد كل الجداول
- **feat(visits):** السماح للمدير بأن يكون زائراً للحلقة بصفته مديراً — قائمة «الزائر» تشمل المدير + المشرفين (المدير معيَّن افتراضيّاً)، دون أي تعديل على قاعدة البيانات (RLS يسمح أصلاً)
- **refactor(ui):** استخراج مكوّنات واجهة مشتركة (`PageHeader` و`StatCard` و`EmptyState` و`FilterChips` و`ConfirmModal` و`DataTable`) وإعادة صياغة كل الصفحات لتستخدمها — إزالة تكرار العلامات والأنماط. لفّ مناطق البيانات بـ `<ClientOnly>` لتفادي خطأ ترطيب الجداول. الإبقاء على نهج التوكنز
- **feat(visits):** الزيارات الإشرافية (§4.15) — بطاقات إحصائية + جدولة زيارة + تنفيذها برفع النتائج + تصفية + حذف (المدير). المشرف يدير زياراته (RLS)
- **feat(exam-plan):** خطة الاختبارات (§4.11) — عرض المجموعات (مرحلي/تجميعي) كصفّ لكل مجموعة + إضافة/تعديل/حذف (المدير يكتب صفّي المرحلتين معاً، البقية للاطّلاع). 12 صفّاً مزروعاً = 6 مجموعات
- **feat(students):** دليل الطلاب (§4.23) — قائمة + إضافة/تعديل/حذف وربط بالحلقة، تصفية بالحالة والحلقة (المدير يدير الكل، المعلّم طلاب حلقته، الحذف للمدير). ملف الطالب الكامل لاحقاً
- **feat(auth):** إنفاذ تعطيل الحساب (الخيار 1) — منع دخول الحساب المعطَّل (`useAuth.signIn` يفحص `status` ويُخرج فوراً ويرمي `account_disabled`) + طرد المستخدم إن عُطّل أثناء الجلسة (watcher في الهيكل العام). الحظر على مستوى Supabase Auth (الخيار 2) لاحقاً
- **feat(halqat):** الحلقات (§4.13) — قائمة بطاقات + إنشاء/تعديل/حذف وتعيين معلّم ومشرف، تصفية بالجنس والحالة، عدّ الطلاب (للمدير، client-side عبر RLS). شريط الإنجاز لاحقاً مع التقارير
- **feat(users):** إدارة المستخدمين (§4.12) — قائمة العاملين + تصفية بالدور + تفعيل/تعطيل (للمدير). جزئي: التعديل/العرض/إعادة الدعوة لاحقاً
- **feat(users):** مسار خادمي `POST /api/users` ينشئ حساب المصادقة (بصلاحية الخدمة، مؤكَّد البريد) + ملف profiles، مع تحقّق أن الطالب ليس مستخدماً ومنع غير المدير (`server/utils/supabaseAdmin.ts`)
- **feat(shell):** الهيكل العام `layouts/dashboard.vue` (§4.2) — Sidebar حسب الدور + Topbar + Drawer للجوال + تبديل الوضع + خروج
- **feat(auth):** middleware عامّ `auth.global.ts` (§4.3) لحماية المسارات (تحويل غير المسجّل للدخول، والمسجّل بعيداً عن /login)
- **feat(dashboard):** لوحة المدير (§4.3) — بطاقات إحصائية بأعداد حقيقية + بطاقات إعدادات مربوطة بجدول `app_settings` (قراءة/اعتماد)
- **feat(types):** توليد أنواع TypeScript من قاعدة Supabase إلى `app/types/database.types.ts` (typed client) + `useProfile` (دور المستخدم)
- **feat(auth):** شاشة تسجيل الدخول (§4.1) بمكوّنات Nuxt UI — بريد فقط (الطالب لا يدخل)، تحقّق، إظهار/إخفاء كلمة المرور، نافذة استعادة (إرسال رابط)، تبديل الوضع
- **feat(auth):** composable `useAuth` (دخول/خروج/استعادة عبر Supabase) + `useErrorHandler` (رسائل عربية موحّدة)
- **chore:** لوحة مؤقّتة `pages/dashboard.vue` (تُستبدل بالـ Shell ولوحات الأدوار)

### 2026-06-22
- **feat(landing):** بناء صفحة Landing (§4.0) كـ Nuxt نظيف من التصميم — RTL، تبديل الوضع، SEO، تجاوب، بالتوكنز. الأخبار بيانات وهمية (تُربط بـ §4.26 لاحقاً)
- **feat(design):** تطبيق أول migration على Supabase (المخطّط كامل + RLS) + توسيع `main.css` لمجموعة توكنز التصميم v1.0 الكاملة (فاتح/ليلي) + أصول العلامة في `public/`
- **chore:** تنظيف القالب (حذف AppLogo/TemplateMenu) + جذر `app.vue` نظيف (UApp فقط)
- **fix(ci):** إصلاح ترتيب مفاتيح `nuxt.config.ts` ليمرّ فحص lint (سير عمل GitHub Actions CI)
- **chore(assets):** إضافة نسخ اللوغو الأربع في `public/logo/` (navy/white/blue/green)
- **chore:** سَقالة Nuxt 4 (قالب Nuxt UI) + إضافة Pinia و@nuxtjs/supabase
- **chore:** بنية مجلدات `app/` (composables, constants, layouts, middleware, stores, types, components/ui) + `server/` + `supabase/migrations/`
- **chore:** نقل المشروع إلى `~/Developer/turjuman` + تهيئة git (فرع main) + `.gitignore`/`.env.example` آمنان
- **feat(design):** نظام التصميم في `app/assets/css/main.css` — توكنز §5 (فاتح/ليلي)، خط Lama Sans (300/500/600 مع ربط 400/700)، تدرّجات navy/blue/green/err/neutral
- **chore:** ربط ألوان Nuxt UI بالهوية في `app/app.config.ts` + ضبط RTL/عربي في `nuxt.config.ts`

### (بداية المشروع)
- **docs:** تجهيز ملفات التوثيق الأساسية (CLAUDE.md, ROADMAP.md, DATABASE.md, PAGES.md, FEATURES.md, DESIGN-BRIEF.md)
